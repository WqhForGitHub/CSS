
看到本章的标题你可能一愣，表格布局？这不正是我们极力避免的吗？确实如此，但本章讨论的不是使用表格布局，而是通过 CSS 排布表格的方式。这个话题看似简单，实则复杂得多。

与文档布局中的其他内容相比，表格不同寻常。在弹性盒和栅格出现之前，只有表格能把不同尺寸的元素整齐排列在一起。例如，不管单元格中的内容是多是少，一行中的所有单元格都具有相同的高度。在同一列中，单元格的宽度也是如此。相邻的单元格可以共用一个边框，即使两个单元格的边框样式差异巨大。在阅读本章的过程中你会发现，这些特性是由表格特有的行为和规则（很多是历史遗留问题）决定的。

<br>

# 1. 表格格式化

在深究单元格的边框是如何绘制的、表格的尺寸是如何确定的之前，先来了解表格的基本构成，以及表格中元素之间的关系。这方面的知识称为表格格式化，它与表格布局是完全不同的话题，只有格式化完成后才能开始布局。

<br>

## 1. 表格的视觉排布

首先要知道如何通过 CSS 定义表格的排布方式。这是基础，掌握之后才能知道如何以最好的方式装饰表格。

对 CSS 而言，表格元素和表格内部元素是两个不同的概念。在 CSS 中，表格内部元素生成矩形框，有内容、内边距和边框，但是没有外边距。因此，外边距无法定义两个单元格之间的间隔。遵守 CSS 规范的浏览器将忽略应用到单元格、行和其他表格内部元素（表题例外，参见 14.1.5 节）上的外边距。

表格排布有六条基本规则。这些规则的基础是单元格，即由绘制表格的栅格线围成的区域。在下图中的两个表格里，栅格单元以虚线表示。

![](https://front-end-1257950569.cos.ap-guangzhou.myqcloud.com/CSS%20%E6%9D%83%E5%A8%81%E6%8C%87%E5%8D%97%EF%BC%88%E7%AC%AC4%E7%89%88%EF%BC%89/%E7%AC%AC14%E7%AB%A0%EF%BC%9ACSS%20%E4%B8%AD%E7%9A%84%E8%A1%A8%E6%A0%BC%E5%B8%83%E5%B1%80/%E8%A1%A8%E6%A0%BC%E5%B8%83%E5%B1%80%E7%9A%84%E5%9F%BA%E7%A1%80%E6%98%AF%E6%A0%85%E6%A0%BC%E5%8D%95%E5%85%83.png)

在简单的 2 ⨉ 2 表格中，例如上图中左边那个，栅格单元对应的就是单元格。在复杂的表格中，例如上图中右边那个，有些单元格跨多个栅格单元，但是请注意，每个单元格的边界都沿着栅格单元的边界放置。

栅格单元大都是理论上的结果，无法装饰，甚至不能通过文档对象模型（DOM）访问。我们只是通过它说明，在 CSS 的语义下，表格是如何构成的。

<br>

### 表格排布规则

* 一个行框包含一个由栅格单元构成的行。表格中的全部行框按出现在文档源码中的顺序从上到下排列（不过表头行框或表脚行框例外，它们分别出现在表格的开头和结尾）。因此，表格中栅格行的数量与行元素（例如 tr 元素）的数量相等。
* 一个行组框的栅格单元就是行组中各行框包含的栅格单元。
* 一个列框包含一个或多个由栅格单元构成的列。列框按照出现在文档源码中的顺序依次排列。在从左至右书写的语言中，第一个列框位于左侧，在从右至左书写的语言中则位于右侧。
* 一个列组框包含的栅格单元就是列组中各列框包含的栅格单元。
* 虽然单元格可能跨多行或多列，但是 CSS 没有定义具体方式。这一方面由编写文档的语言完成。跨行或跨列的单元格生成一个矩形框，其宽度和高度等于一个或多个栅格单元的宽度和高度。位于这个矩形顶部的行在跨行或跨列的栅格单元的父级行中。在从左至右书写的语言中，这个单元格的矩形框必须在最左边，但是不能与其他单元格的矩形框重叠。而且，这个矩形框必须在文档源码中同一行中靠前的单元格的右侧（对从左至右书写的语言而言）。在从右至左书写的语言中，跨行或跨列的单元格必须在最右边，不能与其他单元格重叠，而且必须在文档源码中同一行中靠前的单元格左侧。
* 单元格的矩形框不能超出表格或行组的最后一个行框。如果表格的结构可能导致这样的结果，单元格必须变小，直至能在表格或行组里放下为止。

>CSS 规范不建议（但也没禁止）定位单元格和其他表格内部元素。比如说，定位包含跨行单元格的行将显著改变表格的布局，这会把行从表格中完全移除，从而在其他行的布局中无需考虑那个跨行的单元格。不过，在目前的浏览器中完全可以定位表格元素。

按照定义，栅格单元都是矩形的，但不一定具有相同的尺寸。一列中的所有栅格单元具有相同的宽度，一行中的所有栅格单元具有相同的高度。但是不同行中的栅格单元不一定具有相同的高度，不同列中的栅格单元也不一定具有相同的宽度。

了解这些基本规则之后，你可能心生一个疑问：那么到底如何确定一个元素是不是单元格？

<br>

## 2. 设定显示方式的值

在 HTML 中，很容易知道哪些元素是表格的组成部分，因为浏览器能处理 tr 和 td 等元素。而在 XML 中，无法确定哪些元素是表格的一部分。此时就要使用 display 的一系列值了。

```css
display

取值：[ <display-outside> || <display-inside> ] | <display-listitem> | <display-internal> | <display-box> | <display-legacy>
定义：见下
初始值：inline
适用于：所有元素
计算值：指定的值
继承性：否
动画性：否

<display-outside>
	block | inline | run-in
<display-inside>
	flow | flow-root | table | flex | grid | ruby
<display-listitem>
	list-item && <display-outside>? && [ flow | flow-root ]?
<display-internal>
    table-row-group | table-header-group | table-footer-group | table-row | table-cell | table-column-group | table-column | 		table-caption | ruby-base | ruby-text | ruby-base-container | ruby-text-container
<display-box>
	contents | none
<display-legacy>
	inline-block | inline-list-item | inline-table | inline-flex | inline-grid
```

本章只讨论与表格有关的值，其他值则与表格无关。下面简要说明与表格有关的值：

table

​	把元素定义为块级表格。即，定义一个生成块级框的矩形框。显然，对应的 HTML 元素是 table。

inline-table

​      把元素定位为行内表格。即，定义一个生成行内框的矩形框。在与表格无关的值中，与之最接近的是 inline-block。最接近的 HTML 元素是 table，然而 		 HTML 中的表格默认不是行内元素。

table-row

​     把元素定义为由单元格构成的行。对应的 HTML 元素是 tr。

table-row-group

​     把元素定义为由一行或多行构成的行组。对应的 HTML 元素是 tbody。

table-header-group

​     与 table-row-group 十分相似，不过在视觉上，表头行组始终显示在其他行或行组前面，并且显示在上表题后面。如果表格要分为多页打印，用户代理可能会在每一页的顶部重复渲染表头行组（Firebox 就会这么做）。规范没有定义把 table-header-group 应用到多个元素上会得到什么结果。表头行组可以包含多行。对应的 HTML 元素是 thead。

table-footer-group

​     与 table-header-group 十分相似，不过表脚行组始终显示在其他行和行组后面，并且显示在下表题前面。如果表格要分为多页打印，用户代理可能会在每一页的底部重复渲染表脚行组。规范没有定义把 table-footer-gorup 应用到多个元素上会得到什么结果。对应的 HTML 元素是 tfoot。

table-column

​     把元素声明为由单元格构成的列。在 CSS 中，把元素的 display 属性设为这个值没有任何视觉效果，好似设为 none 一样。这个值的作用基本上是定义列中单元格的表现。对应的 HTML 元素是 col。

table-column-group

​    把元素声明为有一列或多列构成的列组。与 table-column 元素一样，table-column-group 元素也不渲染，而是用于定义列组中元素的表现。对应的 HTML 元素是 colgroup。

table-cell

​    把元素定义为表格中的一个单元格。HTML 中的 th 和 td 都是应用 table-cell 的元素。

table-caption

​     定义表题。CSS 没有规定有多个元素使用 caption 时该怎么处理，但是明确提醒了，创作人员不应该在表格或行内表格中的多个元素上应用 display: caption。

<br>

CSS2.1 规范的附录 D 给出了一个应用于 HTML 4.0 的样式表，摘录如下，从中可以一窥这些值的作用：

```css
table {
    display: table;
}

tr {
    display: table-row;
}

thead {
    display: table-header-group;
}

tbody {
    display: table-row-group;
}

tfoot {
    display: table-footer-group;
}

col {
    display: table-column;
}

colgroup {
    display: table-column-group;
}

td,th {
    display: table-cell;
}

caption {
    display: table-caption;
}
```

<br>

XML 中的元素默认没有显示方式语义，这些值的作用就体现出来了。请看下面的标记：

```xml
<scores>
	<headers>
        <label>Team</label>
        <label>Score</label>
    </headers>
    <game sport="MLB" league="NL">
        <team>
            <name>Reds</name>
            <score>8</score>
        </team>
        <team>
            <name>Cubs</name>
            <score>5</score>
        </team>
    </game>
</scores>
```

这段标记可以使用下述样式格式化为表格数据：

```css
scores {
    display: table;
}

headers {
    display: table-header-group;
}

game {
    display: table-row-group;
}

team {
    display: table-row;
}

label, name, score {
    display: table-cell;
}
```

各单元格可以根据需要进一步装饰，比如说加粗 label 元素，右对齐 scores 元素。

<br>

### 行主导

CSS 中的表格模型是行主导的。也就是说，这个模型假设创作人员在使用标记语言时会显式声明行。而列则从行中单元格的布局衍生而来。因此，第一列由每一行中的第一个单元格构成，第二列由每一行中的第二个单元格构成，以此类推。

行主导对 HTML 而言没什么大问题，因为这门标记语言本身就是行导向的。而在 XML 中，影响可就不是一点点了，因为 XML 对创作人员定义表格标记的方式有诸多限制。鉴于 CSS 的表格模型是行主导的，在以列为准的标记语言中基本上是无法实现表格布局的（假设文档的表现使用 CSS 定义）。

<br>

### 列

虽然 CSS 的表格模型是行主导的，但是列在布局中仍有它的作用。单元格可以同时属于两个上下文（行和列），尽管在文档源码中单元格是行的后代。然而，在 CSS 中，行和行组只能应用四个与表格无关的属性：border、background、width 和 visibility。

此外，这四个属性还有一些只在列上下文中生效的特殊规则：

border

​      仅当 border-collapse 属性的值为 collapse 时才能为列和列组设置边框。此时，根据折叠算法在列和行组中各单元格的边界处绘制边框（见 14.2.2 节）。

background

​      仅当单元格及所在的行是透明背景时列和列祖的背景才可见（见 14.1.4 节）。

width

​      width 属性定义列或列组的最小宽度。列（或列组）中单元格的内容可能会迫使列变宽。

visibility

​      如果列或列组的 visibility 属性值为 collapse，那么列（或列组）中的单元格都不渲染。从折叠的列跨刀其他列的单元格将被裁剪，好似从其他列跨到隐藏的列一样。此外，表格的总宽度将减去折叠的列所占的宽度。对列或列组而言，除了 hidden 之外，其他的 visibility 值都将被忽略。

<br>

## 3. 匿名表格对象

有些标记语言可能没有足够的元素能完整表示 CSS 定义的表格，而且有时创作人员会忘记编写所需的全部元素。以下面的 HTML 为例：

```html
<table>
    <td>Name: </td>
    <td><input type="text"></td>
</table>
```

扫一眼这段标记，你可能认为它定义了一个表格，表格中有一行，那一行里有两个单元格。但是，从结构上看，这段标记没有定义行的元素（缺少 tr）。

为了覆盖这种情况，CSS 定义了一种插入机制：以匿名对象的形式插入缺少的表格组件。为了了解这个机制的基本原理，我们再看一下前面缺少行元素的 HTML 示例。在 CSS 上下文中，table 元素及作为其后代的单元格之间将插入一个匿名表格行对象：

```html
<table>
    <!-- 匿名表格行对象开始 -->
    <td>Name: </td>
    <td><input type="text"></td>
    <!-- 匿名表格行对象结束 -->
</table>
```

这个过程的图解如下图所示，图中的虚线表示插入的域名表格行。

![](https://front-end-1257950569.cos.ap-guangzhou.myqcloud.com/CSS%20%E6%9D%83%E5%A8%81%E6%8C%87%E5%8D%97%EF%BC%88%E7%AC%AC4%E7%89%88%EF%BC%89/%E7%AC%AC14%E7%AB%A0%EF%BC%9ACSS%20%E4%B8%AD%E7%9A%84%E8%A1%A8%E6%A0%BC%E5%B8%83%E5%B1%80/%E8%A1%A8%E6%A0%BC%E6%A0%BC%E5%BC%8F%E5%8C%96%E8%BF%87%E7%A8%8B%E4%B8%AD%E7%94%9F%E6%88%90%E7%9A%84%E5%8C%BF%E5%90%8D%E5%AF%B9%E8%B1%A1.png)

CSS 的表格模型可能插入七种不同的域名对象。类似于继承和特指度，下述七条规则也试图对 CSS 的表现方式赋予一种直观意义。

<br>

### 对象插入规则

1. 如果 table-cell 元素的父元素不是 table-row，在 table-cell 和其父元素之间插入一个匿名 table-row 对象。插入的对象将包含 table-cell 元素的全部后续同辈元素。请看下述样式和标记：

```css
system {
    display: table;
}

name, moons {
    display: table-cell;
}
```

```xml
<system>
	<name>Mercury</name>
    <moons>0</moons>
</system>
```

匿名 table-row 对象插在单元格元素和 system 元素之间，而且包含 name 和 moons 元素。

<br>

父元素为 table-row-group 时上述规则也成立。在前例的基础上，假设扩增为下面这样：

```css
system {
    display: table;
}

planet {
    display: table-row-group;
}

name, moons {
    display: table-cell;
}
```

```xml
<system>
	<planet>
        <name>Mercury</name>
        <moons>0</moons>
    </planet>
    <planet>
        <name>Venus</name>
        <moons>0</moons>
    </planet>
</system>
```

在这个例子中，两组单元格都将包含在插在它们和 planet 元素之间的匿名 table-row 对象里。

2. 如果 table-row 元素的父元素不是 table、inline-table 或 table-row-group，在 table-row 元素和其父元素之间插入一个匿名 table 元素。插入的对象将包含 table-row 元素的全部后续同辈元素。请看下述样式和标记：

```css
docbody {
    display: block;
}

planet {
    display: table-row;
}
```

```html
<docbody>
	<planet>
        <name>Mercury</name>
        <moons>0</moons>
    </planet>
    <planet>
        <name>Venus</name>
        <moons>0</moons>
    </planet>
</docbody>
```

因为 planet 元素的父元素的 display 值是 block，所以在 planet 元素和 docbody 元素之间将插入一个匿名 table 对象。插入的匿名 table 对象将包含两个 planet 元素，因为它们是连续的同辈元素。

3. 如果 table-column 元素的父元素不是 table、inline-table 或 table-column-group，在 table-column 元素和其父元素之间将插入一个匿名 table 对象。这与前面所述的 table-row 规则基本一样，只不过现在变成列导向了。
4. 如果 table-row-group、table-header-group、table-footer-group、table-column-group 或 table-caption 元素的父元素不是 table，在这些元素和其父元素之间插入一个匿名 table 对象。
5. 如果 table 或 inline-table 元素的子元素不是 table-row-group、table-header-group、table-footer-group、table-row 或 table-caption，在 table 元素和其子元素之间插入一个匿名 table-row 对象。插入的匿名对象包含 table-row-group、table-header-group、table-footer-group、table-row 或 table-caption 之外的子元素的全部后续同辈元素。请看下述标记和样式：

```css
system {
    display: table;
}

planet {
    display: table-row;
}

name, moons {
    display: table-cell;
}
```

```html
<system>
	<planet>
        <name>Mercury</name>
        <moons>0</moons>
    </planet>
    <name>Venus</name>
    <moons>0</moons>
</system>
```



这里，在 system 元素与第二组 name 和 moons 元素之间将插入一个匿名 table-row 对象。planet 元素不在这个匿名对象中，因为它的 display 属性为 table-row。

6. 如果 table-row-group、table-header-group 或 table-footer-group 的子元素不是 table-row，在它们和其子元素插入一个匿名 table-row 对象。插入的匿名对象包含子元素的全部后续同辈元素（table-row 对象自身除外）。请看下述标记和样式：

```css
system {
    display: table;
}

planet {
    display: table-row-group;
}

name, moons {
    display: table-cell;
}
```

```html
<system>
	<planet>
        <name>Mercury</name>
        <moons>0</moons>
    </planet>
    <name>Venus</name>
    <moons>0</moons>
</system>
```

这里，每一组 name 和 moons 元素都将包含在一个匿名 table-row 对象中。第二组满足规则 5 所述的插入条件。对第一组来说，匿名对象插在 planet 元素和其子元素之间，因为 planet 是个 table-row-group 元素。

7. 如果 table-row 元素的子元素不是 table-cell，在元素和其子元素之间插入一个匿名 table-cell 对象。插入的匿名对象包含子元素的全部后续同辈元素（table-cell 元素自身除外）。请看下述标记和样式：

```css
system {
    display: table;
}

planet {
    display: table-row;
}

name, moons {
    display: table-cell;
}
```

```html
<system>
	<planet>
        <name>Mercury</name>
        <num>0</num>
    </planet>
</system>
```

因为 num 元素的 display 值与表格无关，所以在 planet 元素和 num 元素之间将插入一个匿名 table-cell 对象。

这个行为还涵盖匿名行内框。假设标记中没有 num 元素：

```html
<system>
	<planet>
        <name>Mercury</name>
        0
    </planet>
</system>
```

此时，0 仍将包含在一个匿名 table-cell 对象中。为了进一步说明这一点，下面再举一个例子。这个例子改编自 CSS 规范：

```css
example {
    display: table-cell;
}

row {
    display: table-row;
}

hey {
    font-weight: 900;
}
```

```html
<example>
	<row>This is the <hey>top</hey> row.</row>
    <row>This is the <hey>bottom</hey> row.</row>
</example>
```

在每个 row 元素中，文本片段和 hey 元素将包含在各自的匿名 table-cell 对象中。

<br>

## 4. 表格中的层

为了完整表示一个表格，CSS 定义了 6 个独立的层，表格的不同部分分别放在不同的层上。这几个层如下图所示。

![](https://front-end-1257950569.cos.ap-guangzhou.myqcloud.com/CSS%20%E6%9D%83%E5%A8%81%E6%8C%87%E5%8D%97%EF%BC%88%E7%AC%AC4%E7%89%88%EF%BC%89/%E7%AC%AC14%E7%AB%A0%EF%BC%9ACSS%20%E4%B8%AD%E7%9A%84%E8%A1%A8%E6%A0%BC%E5%B8%83%E5%B1%80/%E8%A1%A8%E7%A4%BA%E8%A1%A8%E6%A0%BC%E6%97%B6%E9%87%87%E7%94%A8%E7%9A%84%E6%A0%BC%E5%BC%8F%E5%8C%96%E5%B1%82.png)

一般来说，表格不同部分的样式在各自所属的层上渲染。因此，如果 table 元素有绿色的背景和一像素宽的黑色边框，那么这些样式在最底下一层渲染。针对列组的样式在倒数第二层上渲染，针对列的样式在倒数第三层渲染，以此类推。对应于单元格的最上层在最后绘制。

总体而言，这样的处理方式是符合逻辑的，毕竟你会想让单元格的背景在表格元素的背景之上绘制。上图揭示的最重要的一点是，列的样式在行的样式下面，因此行的背景将覆盖列的背景。

谨记一点，默认情况下所有元素的背景都是透明的。因此，对下述标记来说，表格元素的背景透过单元格、行、列等自身没有背景的元素是可见的，如下图所示：

```html
<table style="background: #B84;">
    <tr>
        <td>hey</td>
        <td style="background: #ABC;">there</td>
    </tr>
    <tr>
        <td>what's</td>
        <td>up?</td>
    </tr>
    <tr style="background: #CBA;">
        <td>not</td>
        <td style="background: #ECC;">much</td>
    </tr>
</table>
```

![](https://front-end-1257950569.cos.ap-guangzhou.myqcloud.com/CSS%20%E6%9D%83%E5%A8%81%E6%8C%87%E5%8D%97%EF%BC%88%E7%AC%AC4%E7%89%88%EF%BC%89/%E7%AC%AC14%E7%AB%A0%EF%BC%9ACSS%20%E4%B8%AD%E7%9A%84%E8%A1%A8%E6%A0%BC%E5%B8%83%E5%B1%80/%E9%80%8F%E8%BF%87%E5%85%B6%E4%BB%96%E5%B1%82%E7%9C%8B%E5%88%B0%E8%A1%A8%E6%A0%BC%E6%A0%BC%E5%BC%8F%E5%8C%96%E5%B1%82%E7%9A%84%E8%83%8C%E6%99%AF.png)

<br>

## 5. 表题

顾名思义，表题是一小段描述表格中是什么内容的文本。因此，2016 年第 4 季度股票行情表的表题内容可能是 2016 年第 4 季度股票行情。借助 caption-side 属性，可以把表题放在表格上方或下方，而不管表题在表格的结构中位于何处（在 HTML5 中，caption 元素只能作为 table 元素的第一个子元素，不过其他语言可能有不同的规则）。

```css
caption-side

取值：top | bottom
初始值：top
适用于：display 属性的值为 table-caption 的元素
计算值：指定的值
继承性：是
动画性：否
备注：CSS2 中有 left 和 right 两个值，但是由于缺少支持，CSS2.1 去掉了
```

表题有点奇怪，至少视觉上如此。CSS 规范指出，表题就像一个放在表格框之前（或之后）的块级框一样，只是，表题还从表格继承样式。

一个简单的例子就足以展示表题表现上最为重要的一些方面。请看下面的样式，结果如下图所示：

```css
caption {
    background: #B84;
    margin: 1em 0;
    caption-side: top;
}

table {
    color: white;
    background: #840;
    margin: 0.5em 0;
}
```

caption 元素中的文本从表格那里继承 color 属性，颜色为 white，而表题自身还有背景。表格的边框外边界与表题的外边距外边界之间的间隔为 1em，因为表格的上外边距和表题的下外边距折叠了。最后，表题的宽度由 table 元素中内容的宽度决定，可见表格元素是表题的容纳块。

![](https://front-end-1257950569.cos.ap-guangzhou.myqcloud.com/CSS%20%E6%9D%83%E5%A8%81%E6%8C%87%E5%8D%97%EF%BC%88%E7%AC%AC4%E7%89%88%EF%BC%89/%E7%AC%AC14%E7%AB%A0%EF%BC%9ACSS%20%E4%B8%AD%E7%9A%84%E8%A1%A8%E6%A0%BC%E5%B8%83%E5%B1%80/%E8%A3%85%E9%A5%B0%E8%A1%A8%E9%A2%98%E5%92%8C%E8%A1%A8%E6%A0%BC.png)

把 caption-side 的值改为 bottom 之后结果还是一样，只是表题将出现在表格框的下方，而且折叠的是表题的上外边距和表格的下外边距。

多数时候，表题都像块级元素那样装饰，可以设定内边距、边框、背景等。例如，如果想改变表题中文本的横向对齐方式，可以使用 text-align 属性。因此，如果想右对齐前例中的表题，可以这样写：

```css
caption {
    background: gray;
    margin: 1em 0;
    caption-side: top;
    text-align: right;
}
```

<br>

# 2. 单元格的边框

在 CSS 中有两种完全不同的边框模型。分离边框模型在单元格的布局之间有间隔时起作用。另一个选项是折叠边框模型，此时单元格之间在视觉上没有间隔，单元格之间的边框将合并或折叠在一起。现在前者是默认模型，而在以前的 CSS 版本中，后者是默认模型。

创作人员可以使用 border-collapse 属性在这两个模型之间做出选择。

```css
border-collapse

取值：collapse | separate | inherit
初始值：separate
适用于：display 属性的值为 table 或 table-inline 的元素
继承性：是
计算值：指定的值
备注：在 CSS2 中，默认值为 collapse
```

这个属性的唯一作用是告知用户代理该采用哪种边框模式。设为 collapse 值时，用户代理使用折叠边框模式。设为 separate 值时，用户代理使用分离边框模式。先讲后一个模型，因为它更容易说明，而且是默认值。

<br>

## 1. 分离单元格的边框

在这种模型中，表格中的每个单元格都与其他单元格相隔一定的距离，而且单元格之间的边框不会折叠在一起。因此，下述样式和标记将得到如下图所示的结果：

```css
table {
    border-collapse: separate;
}

td {
    border: 3px double black;
    padding: 3px;
}

tr:nth-child(2) td:nth-child(2) {
    border-color: gray;
}
```

```html
<table cellspacing="0">
    <tr>
        <td>cell one</td>
        <td>cell two</td>
    </tr>
    <tr>
        <td>cell three</td>
        <td>cell four</td>
    </tr>
</table>
```

注意，相邻单元格的边框挨在一起，但彼此是独立的。在图中你会看到单元格之间有三条线，那是两条双线边框相邻导致的。第四个单元格的灰色边框能更清楚地表明这一点。

![](https://front-end-1257950569.cos.ap-guangzhou.myqcloud.com/CSS%20%E6%9D%83%E5%A8%81%E6%8C%87%E5%8D%97%EF%BC%88%E7%AC%AC4%E7%89%88%EF%BC%89/%E7%AC%AC14%E7%AB%A0%EF%BC%9ACSS%20%E4%B8%AD%E7%9A%84%E8%A1%A8%E6%A0%BC%E5%B8%83%E5%B1%80/%E5%88%86%E7%A6%BB%E7%9A%84%EF%BC%88%E5%9B%A0%E6%AD%A4%E6%98%AF%E7%8B%AC%E7%AB%8B%E7%9A%84%EF%BC%89%E5%8D%95%E5%85%83%E6%A0%BC%E8%BE%B9%E6%A1%86.png)

前例中的 HTML 设定了 cellspacing 属性，这是为了确保单元格之间没有间隔，但这样得到的实际效果似乎不太好。转念一想，如果能定义彼此独立的边框，那么通过 CSS 就应该有办法调整单元格的间距。是的，确实有。

<br>

### 边框间距

把单元格的边框区分开以后，有时你可能还想让边框相隔一定的距离。利用 border-spacing 属性可以轻易做到这一点。这个属性的作用比 HTML 属性 cellspacing 强大，完全可以取而代之。

```css
border-spacing

取值：<length> <length>?
初始值：0
适用于：display 属性的值为 table 或 table-inline 的元素
计算值：两个绝对长度
继承性：是
动画性：是
备注：如果 border-collapse 属性的值不是 separate，忽略这个属性
```

这个属性的值可以是一个或两个长度。如果想让所有单元格都相距 1 像素，声明 border-spacing: 1px，就行了。而如果想让单元格在横向上相距 1 像素、纵向上相距 5 像素，则声明 border-spacing: 1px 5px。提供两个值时，第一个值始终是横向间隔，第二个值始终是纵向间隔。

<br>

间隔值在表格外侧的单元格与 table 元素的内边距之间也存在。因此，下述样式将得到如下图所示的结果：

```css
table {
    border-collapse: separate;
    border-spacing: 5px 8px;
    padding: 12px;
    border: 2px solid black;
}

td {
    border: 1px solid gray;
}

td#squeeze {
    border-width: 5px;
}
```

![](https://front-end-1257950569.cos.ap-guangzhou.myqcloud.com/CSS%20%E6%9D%83%E5%A8%81%E6%8C%87%E5%8D%97%EF%BC%88%E7%AC%AC4%E7%89%88%EF%BC%89/%E7%AC%AC14%E7%AB%A0%EF%BC%9ACSS%20%E4%B8%AD%E7%9A%84%E8%A1%A8%E6%A0%BC%E5%B8%83%E5%B1%80/%E8%BE%B9%E6%A1%86%E9%97%B4%E8%B7%9D%E5%9C%A8%E5%8D%95%E5%85%83%E6%A0%BC%E5%92%8C%E6%89%80%E5%B1%9E%E7%9A%84%E8%A1%A8%E6%A0%BC%E4%B9%8B%E9%97%B4%E4%B9%9F%E5%AD%98%E5%9C%A8.png)

上图中，相邻的单元格在横向上有 5 像素的间距，因而在最右边和最左边的单元格的边框与 table 元素的右边框和左边框之间有 17 像素的间距。类似地，相邻地单元格在纵向上相距 8 像素，因而在最上面和最下面两行中的单元格与表格的上边框和下边框之间有 20 像素的间距。在一个表格中，单元格边框之间的间距是恒定不变的，不管单元格的边框有多宽。

另外注意，border-spacing 值在表格中声明，而不是在单个单元格上声明。如果前例为 td 元素声明 border-spacing，其值将被忽略。

在分离边框模型中，不能为行、行组、列和列组设定边框。在遵守 CSS 规范的用户代理中，为这些元素设置的边框都将被忽略。

<br>

### 处理空单元格

一个表格中的每个单元格在视觉上都是不同的，那么空单元格（即里面没有内容）该怎么处理？你有两个选择，由 empty-cells 属性的值确定。

```css
empty-cells

取值：show | hide
初始值：show
适用于：display 属性的值为 table-cell 的元素
计算值：指定的值
继承性：是
动画性：否
备注：如果 border-collapse 属性的值不是 separate，忽略这个属性
```

如果把 empty-cells 属性的值设为 show，空单元格的边框和背景往常一样渲染，就像单元格中有内容一样。如果把值设为 hide，单元格的任何部分都不渲染，好似设定 visibility: hidden 一样。

只要单元格中有内容，就不能视作空单元格。这里所说的内容，不只包括文本、图像、表单元素等，还有不间断空格实体（`&nbsp;`），以及除 CR（回车符）、LF（换行符）、制表符和空格之外的其他空白。如果一行里的所有单元格都是空的，而且 empty-cells 属性的值都是 hide，那么整行就像设置了 display: none 一样。

<br>

## 2. 折叠单元格的边框

折叠边框模型就像 HTML 排布没有间距的单元格一样，它比分离边框模型复杂一些。折叠边框边框与分离边框模型之间的区别体现为以下几点：

* border-collapse 属性的值为 collapse 时，display 属性的值为 table 或 inline-table 的元素不能有内边距，但是可以有外边距。因此，在折叠边框模型中，表格四周的边框与最外层的单元格之间肯定没有间隔。
*  边框可以应用于单元格、行、行组、列和列组。当然，表格自身始终可以有边框。
* 在折叠边框模型中，单元格的边框之间肯定没有间隔。其实，边框在毗邻处折叠在一起，因此只会绘制条折叠的边框。这与外边距折叠有点像，即最大的外边距胜出。单元格的边框在折叠时，占上风的边框胜出。
* 折叠的边框居中放在单元格之间假想的栅格线上。

接下来的两节详细探讨后两点。

<br>

### 折叠边框的布局

为了更好地理解折叠边框模型地工作原理，下面以表格中的一行的布局为例说明，如下图所示。

对每个单元格来说，与预期一样，内边距和内容宽度在边框内侧。而单元格之间的边框，一半在两个单元格之间的栅格线一侧，另一半在栅格线的另一侧。不论如何，单元格的边界处只会绘制一条边框。你可能以为边框一分两半，分别在栅格线两侧绘制，但事实并非如此。

假设中间那个单元格的边框是绿色实线，两侧的单元格的边框是红色实线。此时，中间那个单元格左右两边的边框（与两侧相邻单元格的边框折叠在一起）要么都是绿色，要么都是红色，这取决于哪个边框胜出。下一节讨论如何判断哪个边框胜出。

你可能注意到了，外侧的边框超出了表格的宽度。这是因为，在这个模型中，表格边框的一半含在宽度之内。另一半边框在这个范围之外，位于外边距中。看起来有点奇怪，但这就是折叠边框模型定义的方式。

规范给出了一个公式，转摘如下，以防有人喜欢用公式：

row width = (0.5 * border-width-0) + padding-left-1 + width-1 + padding-right-1 + border-width-1 + padding-left-2 + ... + padding-right-n + (0.5 * border-width-n)

border-width-n 表示第 n 个单元格与下一个单元格之间的边框，因此 border-width-3 表示第三个单元格和第四个单元格之间的边框。n 表示一行中单元格的总数。

这个机制有个小例外。在开始排布折叠边框的表格时，用户代理先为表格的左右边框计算一个初始值。表格左边框的初始宽度等于第一行中第一个单元格左边框的一半，表格右边框的初始宽度等于第一行中最后一个单元格右边框的一半。在后续的行中，如果左边框或右边框比边框的初始宽度大，将凸出到表格的外边距区域。

如果边框的宽度是显示单位（像素、点等）的奇数倍，由用户代理决定如何把边框居中显示在栅格线上。用户代理可能会移动边框，稍微偏离中点，或者向上或向下取整为显示单位的偶数倍，或者使用抗锯齿技术，又或者使用其他合理的方式调整。

<br>

### 边框折叠

相邻的两条或两条以上边框折叠在一起。其实，边框并非真的折叠，而是看哪一条占上风。具体哪条边框胜出，有严格的规则：

* 如果折叠的边框中有一条边框的 border-style 为 hidden，那么那一条边框在折叠的所有边框中优先级最高。这个位置上的边框都将隐藏。
* 如果所有边框都可见，那么较宽的边框比较窄的边框优先级高。因此，如果一条 2 像素宽的点线边框与一条 5 像素宽的双线边框折叠，这个位置上将显示 5 像素宽的双线边框。
* 如果折叠的各个边框具有相同的宽度，但是样式不同，那么优先级从大到小的顺序为：double、solid、dashed、dotted、ridge、outset、groove、inset、none。因此，如果折叠的两条边框宽度相等，其中一条的样式为 dashed，另一条的样式为 ouset，那么这个位置上将显示虚线边框。
* 如果折叠的各个边框具有相同的样式和宽度，但是颜色不同，那么颜色从这些元素中获取，优先级从高到低分别为：单元格、行、行组、列、列祖、表格。因此，如果单元格的边框与列的边框折叠（除颜色之外都相同），那么将使用单元格的边框颜色（以及样式和宽度）。如果折叠的边框属于相同类型的元素，例如两条具有相同样式和宽度，但是颜色不同的行边框，那么颜色取自离顶部和左边较远的边框（在从左至右书写的语言中，否则取自离顶部和右边较远的边框）。

下述样式和标记分别演示上述四条规则，结果如下图所示：

```css
table {
    border-collapse: collapse;
    border: 3px outset gray;
}

td {
    border: 1px solid gray;
    padding: 0.5em;
}

#r2c1, #r2c2 {
    border-style: hidden;
}

#r1c1, #r1c4 {
    border-width: 5px;
}

#r2c4 {
    border-style: double;
    border-width: 3px;
}

#r3c4 {
    border-style: dotted;
    border-width: 2px;
}

#r4c1 {
    border-bottom-style: hidden;
}

#r4c3 {
    border-top: 13px solid silver;
}
```

```html
<table>
    <tr>
        <td id="r1c1">1-1</td>
        <td id="r1c2">1-2</td>
        <td id="r1c3">1-3</td>
        <td id="r1c4">1-4</td>
    </tr>
    <tr>
        <td id="r2c1">2-1</td>
        <td id="r2c2">2-2</td>
        <td id="r2c3">2-3</td>
        <td id="r2c4">2-4</td>
    </tr>
    <tr>
        <td id="r3c1">3-1</td>
        <td id="r3c2">3-2</td>
        <td id="r3c3">3-3</td>
        <td id="r3c4">3-4</td>
    </tr>
    <tr>
        <td id="r4c1">4-1</td>
        <td id="r4c2">4-2</td>
        <td id="r4c3">4-3</td>
        <td id="r4c4">4-4</td>
    </tr>
</table>
```

![](https://front-end-1257950569.cos.ap-guangzhou.myqcloud.com/CSS%20%E6%9D%83%E5%A8%81%E6%8C%87%E5%8D%97%EF%BC%88%E7%AC%AC4%E7%89%88%EF%BC%89/%E7%AC%AC14%E7%AB%A0%EF%BC%9ACSS%20%E4%B8%AD%E7%9A%84%E8%A1%A8%E6%A0%BC%E5%B8%83%E5%B1%80/%E5%AF%B9%E8%BE%B9%E6%A1%86%E5%AE%BD%E5%BA%A6%E3%80%81%E6%A0%B7%E5%BC%8F%E5%92%8C%E9%A2%9C%E8%89%B2%E7%9A%84%E5%A4%84%E7%90%86%E6%96%B9%E5%BC%8F%E5%8F%AF%E8%83%BD%E5%BE%97%E5%88%B0%E4%B8%8E%E5%BE%80%E5%B8%B8%E4%B8%8D%E5%A4%AA%E4%B8%80%E6%A0%B7%E7%9A%84%E7%BB%93%E6%9E%9C.png)

我们来看看每个单元格的情况：

* 单元格 1-1 和 1-4 的边框宽度为 5 像素，比相邻的任何一边边框都宽，因此它们不仅在毗邻的边框中胜出，还战胜了表格的边框。但是单元格 1-1 的下边框例外，这一边的边框被抹除了。
* 单元格 1-1 的下边框之所以被抹除，是因为单元格 2-1 和 2-2 的边框是隐藏的，单元格边界处根本没有边框。同样，表格的边框（单元格 2-1 的左边）也被单元格的边框击败了。单元格 4-1 的下边框也是隐藏的，因此底边没有边框。
* 单元格 2-4 的 3 像素双线边框在顶边被单元格 1-4 的 5 像素实线边框覆盖。而单元格 2-4 的边框又把它与单元格 2-3 之间的边框覆盖了，因为单元格 2-4 的边框更宽，所以占上风。单元格 2-4 还覆盖了它与单元格 3-4 之间的边框，虽然两条边框的宽度相等，但是单元格 2-4 的双线样式比单元格 3-4 的点线边框占上风。
* 单元格 3-3 底边的 13 像素银色边框不只覆盖单元格 4-3 的上边框，还影响两个单元格和所在行中内容的布局。
* 表格外圈没有特别装饰的单元格，其 1 像素宽的实线边框被表格元素的 3 像素外凸边框覆盖。

这些规则看似复杂，但基本上还是符合直觉的，多加练习也不难掌握。不过，需要指出的是，netscape 1.1 时代的表格使用下面这组相当简单的规划就能复现，结果如下图所示：

```css
table {
    border-collapse: collapse;
    border: 2px outset gray;
}

td {
    border: 1px inset gray;
}
```

![](https://front-end-1257950569.cos.ap-guangzhou.myqcloud.com/CSS%20%E6%9D%83%E5%A8%81%E6%8C%87%E5%8D%97%EF%BC%88%E7%AC%AC4%E7%89%88%EF%BC%89/%E7%AC%AC14%E7%AB%A0%EF%BC%9ACSS%20%E4%B8%AD%E7%9A%84%E8%A1%A8%E6%A0%BC%E5%B8%83%E5%B1%80/%E5%A4%8D%E7%8E%B0%E6%97%A7%E6%97%B6%E8%A1%A8%E6%A0%BC%E7%9A%84%E8%A1%A8%E7%8E%B0.png)

<br>

# 3. 表格的尺寸

深入讨论过表格格式和单元格边框的表现之后，接下来要学习表格的尺寸和内部元素。表格的宽度有两种确定方式：固定宽度布局和自动宽度布局。不管使用哪种方式确定宽度，表格的高度都自动计算。

<br>

## 1. 宽度

既然表格的宽度有两种确定方式，那么必然有这么一种方式用于声明具体使用哪种方式。创作人员可以使用 table-layout 属性在这两种宽度计算方式中做出选择。

```css
table-layout

取值：auto | fixed
初始值：auto
适用于：display 属性的值为 table 或 inline-table 的元素
计算值：指定的值
继承性：是
动画性：否
```

虽然在同一个表格上应用这两种模型可能得到不同的结果，但二者之间最显著的区别还是速度。用户代理计算表格的布局时，固定宽度的表格布局要比自动宽度模型快一些。

<br>

### 固定布局

固定布局模型之所以更快，主要原因是无需考虑单元格中的内容。在这种模型中，布局由表格、列和单元格的宽度决定。

* 如果列元素的 width 属性的值不是 auto，设定的就是整列的宽度。
* 如果列的宽度为 auto，但是表格第一行中位于这一列的单元格的 width 属性的值不是 auto，那么这个单元格的宽度就是整列的宽度。如果这个单元格跨多列，其宽度将平分到各列中。
* 如果列的宽度仍是自动确定的，将自动确定，尽可能保持各列宽度相等。

经过上述过程处理之后，表格的宽度将被设为表格的 width 值和各列宽度之和中较大的那个。如果表格的宽度比列的宽度之和大，二者之差除以列数得到的结果将增加到各列上。

这种方式之所以较快，是因为列的宽度由表格的第一行决定。后续各行中的单元格都与第一行确定的列宽度保持一致。后续行中的单元格不改变列的宽度（其实也无法改变），因此未这些单元格设定的 width 值将被忽略。倘若内容在单元格中放不下，overflow 的值决定内容是被裁剪、是可见的，还是显示滚动条。

请看下述样式和标记，结果如下图所示：

```css
table {
    table-layout: fixed;
    width: 400px;
    border-collapse: collapse;
}

td {
    border: 1px solid;
}

col#c1 {
    width: 200px;
}

#r1c2 {
    width: 75px;
}

#r2c3 {
    width: 500px;
}
```

```html
<table>
    <colgroup>
        <col id="c1">
        <col id="c2">
        <col id="c3">
        <col id="c4">
    </colgroup>
    <tr>
        <td id="r1c1">1-1</td>
        <td id="r1c2">1-2</td>
        <td id="r1c3">1-3</td>
        <td id="r1c4">1-4</td>
    </tr>
    <tr>
        <td id="r2c1">2-1</td>
        <td id="r2c2">2-2</td>
        <td id="r2c3">2-3</td>
        <td id="r2c4">2-4</td>
	</tr>
 	<tr>
        <td id="r3c1">3-1</td>
        <td id="r3c2">3-2</td>
        <td id="r3c3">3-3</td>
        <td id="r3c4">3-4</td>
	</tr>
    <tr>
        <td id="r4c1">4-1</td>
        <td id="r4c2">4-2</td>
        <td id="r4c3">4-3</td>
        <td id="r4c4">4-4</td>
	</tr>
</table>
```

从下图中可以看到，第一列的宽度为 200 像素，恰好是表格宽度（400 像素）的一半。第二列的宽度是 75 像素，因为我们为第一行中位于这一列的单元格现实设定了宽度。第三列和第四列的宽度都是 61 像素。为什么？因为第一列和第二列的宽度之和（275 像素）加上两列之间边框的宽度（3 像素）等于 278 像素，400 减去 278 等于 122，而 122 除以 2 等于 61，所以第三列和第四列的宽度是 61 像素。那么为 #r2c3 设定的 500 像素宽度？这个值被忽略，因为该单元格不在表格的第一行中。

![](https://front-end-1257950569.cos.ap-guangzhou.myqcloud.com/CSS%20%E6%9D%83%E5%A8%81%E6%8C%87%E5%8D%97%EF%BC%88%E7%AC%AC4%E7%89%88%EF%BC%89/%E7%AC%AC14%E7%AB%A0%EF%BC%9ACSS%20%E4%B8%AD%E7%9A%84%E8%A1%A8%E6%A0%BC%E5%B8%83%E5%B1%80/%E5%9B%BA%E5%AE%9A%E5%AE%BD%E5%BA%A6%E7%9A%84%E8%A1%A8%E6%A0%BC%E5%B8%83%E5%B1%80.png)

注意，使用固定宽度布局模型时，并非一定要为表格显式设定 width，不过设定的话会有很大的帮助。例如，对下述样式来说，用户代理计算出来的表格宽度比父元素的宽度小 50 像素。在固定布局模型中，计算得到的宽度就用作表格的宽度。

```css
table {
    table-layout: fixed;
    margin: 0 25px;
    width: auto;
}
```

然而，我们不一定非要这么做。用户代理还能使用自动宽度布局模型排布 width 的值为 auto 的表格。

<br>

### 自动布局

虽然自动宽度布局模型没有固定布局的速度快，但是你可能对它更熟悉，因为它与 HTML 常年使用的表格布局模型基本一样。在当下多数用户代理中，把表格的 width 属性设为 auto 就能触发这个模型，而不管 table-layout 是什么值，但是也不一定。

自动布局较慢的原因在于，用户代理要读取完表格中的全部内容才能开始排布。即，用户代理排布表格时要把每个单元格的内容和样式考虑在内。一般来说，这要求用户代理先做些计算，然后反过来再执行一系列计算。

之所以要读取全部内容是因为，与 HTML 表格一样，表格的布局取决于每个单元格中的内容。如果最后一行中某个单元格里有个 400 像素宽的图像，那么上面每个单元格（在同一列中）的宽度都将变成 400 像素。因此，在排布表格之前，每个单元格的宽度都要计算出来，然后再做调整（可能还会导致新一轮宽度计算）。

这种模型的具体过程见下述步骤：

1. 计算一列中每个单元格的最小宽度和最大宽度。
   2. 确定显示内容最少需要多少宽度。在这个过程中，内容可以换行，但是不能超出单元格框。如果为单元格设定了 width 值，而且比最小宽度大，那么单元格的最小宽度将被设为 width 的值。如果单元格的 width 值为 auto，那么单元格的最小宽度将被设为内容的最小宽度。
   3. 最大宽度即不换行（强制换行除外。例如 `<br>` 元素）显示内容所需的宽度。
4. 计算各列的最小宽度和最大宽度。
   	5. 列的最小宽度由那一列中单元格最小宽度最大的那个确定。如果为列显式设定了 width 值，而且比例中每个单元格的最小宽度都大，那么列的最小宽度将被设为 width 的值。
   	6. 列的最大宽度等于那一列中单元格最大宽度值最大的那个。如果为列显式设定了 width 值，而且比列中每个单元格的最大宽度都大，anemic列的最大宽度将被设为 width 的值。这两个行为与传统的 HTML 表格是一样的，即列的宽度将增大到与最宽的单元格相等。
7. 如果单元格横跨多列，那么所跨的那几列的最小宽度之和必须等于单元格的最小宽度。同样，列的最大宽度之和也要等于横跨多列的单元格的最大宽度。用户代理应该把列宽度的变化平均分到所跨的各列中。

此外，用户代理已经知道每一列有多宽了。得到这些信息之后，用户代理才真正开始计算表格的宽度。具体步骤如下：

1. 如果表格宽度的计算值不是 auto，比较表格宽度的计算值与每一列的宽度之和加上边框和单元格间距（使用百分数设定的宽度可能就在此时计算）。二者之中较大的那个就是表格的最终宽度。如果表格宽度的计算值比列宽度、边框和间距之和大，差值除以列数后得到的值将增加到各列上。
2. 如果表格宽度的计算值是 auto，表格的最终宽度等于列宽度、边框和间距之和。这意味着，表格的宽度将正好能放得下内容，这与传统的 HTML 表格是一样的。宽度为百分数的列将以那个百分数为限，但用户代理不一定会满足这个限制。

只有等到最后一步计算完毕之后，用户代理才能排布表格。

下述样式和标记有助于理解上述过程，结果如下图所示：

```css
table {
    table-layout: auto;
    width: auto;
    border-collapse: collapse;
}

td {
    border: 1px solid;
    padding: 0;
}

col#c3 {
    width: 25%;
}

#r1c2 {
    width: 40%;
}

#r2c2 {
    width: 50px;
}

#r2c3 {
    width: 35px;
}

#r4c1 {
    width: 100px;
}

#r4c4 {
    width: 1px;
}
```

```html
<table>
    <colgroup>
        <col id="c1">
        <col id="c2">
        <col id="c3">
        <col id="c4">
    </colgroup>
    <tr>
        <td id="r1c1">1-1</td>
        <td id="r1c2">1-2</td>
        <td id="r1c3">1-3</td>
        <td id="r1c4">1-4</td>
    </tr>
	<tr>
        <td id="r2c1">2-1</td>
        <td id="r2c2">2-2</td>
        <td id="r2c3">2-3</td>
        <td id="r2c4">2-4</td>
    </tr>
	<tr>
        <td id="r3c1">3-1</td>
        <td id="r3c2">3-2</td>
        <td id="r3c3">3-3</td>
        <td id="r3c4">3-4</td>
	</tr>
	<tr>
        <td id="r4c1">4-1</td>
        <td id="r4c2">4-2</td>
        <td id="r4c3">4-3</td>
        <td id="r4c4">4-4</td>
	</tr>
</table>
```

下面依次看一下各列的情况：

* 在第一列中，显式设定了单元格或列宽度的只有单元格 4-1，其宽度为 100px。因为内容较短，所以列的最小宽度和最大宽度都被设为 100px（如果这一列中某个单元格中的内容为几句话，那么列的最大宽度将增加，大到在不换行的情况下能显示全部文本）。
* 在第二列中，我们声明了两个 width 值：把单元格 1-2 的宽度设为 40%，把单元格 2-2 的宽度设为 50px。这一列的最小宽度为 50px，最大宽度为表格最终宽度的 40%。



![](https://front-end-1257950569.cos.ap-guangzhou.myqcloud.com/CSS%20%E6%9D%83%E5%A8%81%E6%8C%87%E5%8D%97%EF%BC%88%E7%AC%AC4%E7%89%88%EF%BC%89/%E7%AC%AC14%E7%AB%A0%EF%BC%9ACSS%20%E4%B8%AD%E7%9A%84%E8%A1%A8%E6%A0%BC%E5%B8%83%E5%B1%80/%E8%87%AA%E5%8A%A8%E8%A1%A8%E6%A0%BC%E5%B8%83%E5%B1%80.png)



* 在第三列中，只有单元格 3-3 显式设定了宽度（35px），不过这一列也设定了 width，值为 25%。因此，这一列的最小宽度为 35 像素，最大宽度为表格最终宽度的 25%。
* 在第四列中，只有单元格 4-4 显式设定了宽度（1px）。这比内容的最小宽度小，因此列的最小宽度和最大宽度相等，都等于单元格中内容的最小宽度。经计算，内容的最小宽度为 22 像素，因此最小宽度和最大宽度都是 22 像素。

现在，用户代理知道四列的最小宽度和最大宽度了，如下所示：

最小宽度 100 像素，最大宽度 100 像素。

最小宽度 50 像素，最大宽度为 40%。

最小宽度 35 像素，最大宽度 25%。

最小宽度 25 像素，最大宽度 22 像素。

因此，表格的最小宽度等于各列的最小宽度加上列之间折叠的边框，即 215 像素。表格的最大宽度等于 123px + 65%，其中 123px 是第一列和最后一列的最大宽度之和，外加共用的折叠边框。最大宽度最终得到的结果为 351.42857142857143 像素（假设 123px 是表格总宽度的 35%）。得到这个结果后，第二列的宽度便可计算出来，等于 140.5 像素，第三列的宽度也能计算出来，等于 87.8 像素。用户代理可能会把这两个值四舍五入为整数，例如 141px 和 88px，也可能不会，这取决于具体使用的渲染方法（上图中给出的是整数）。

注意，用户代理不一定选择使用最大值，有可能会执行其他操作。

这个例子比较简单，也比较容易理解（可能没有看起来那么简单），所有内容的宽度基本是相等的，而且声明的宽度用的都是像素长度。如果表格中有图像、文本段落、表单元素等，确定表格布局的过程可能会变得更为复杂。

<br>

## 2. 高度

表格的宽度费劲千辛万苦才能计算出来，你可能以为计算高度还要复杂一些。其实，对 CSS 而言，这没什么难的，不过浏览器开发者可能不这么认为。

最简单的情况是，通过 height 属性显式设定表格的高度。此时，表格的高度由 height 属性的值定义。这意味着，表格的高度可能会比各列的高度之和大或小。请注意，对表格来说，height 更像是 min-height，因此如果你定义的 height 值比各行的高度之和小，用户代理可能会将其忽略。

反过来，如果表格的 height 值比行的高度之和大，具体如何处理，规范没有规定，而是指出这个问题可能会在以后的 CSS 版本中解决。用户代理可能会增加表格中各行的高度，满足声明的高度值，可能会在表格框中留白，也可能会采用其他完全不同的方式处理。具体方式由用户代理自行决定。

>截至 2017 年年末，用户代理最常采用的方法是增加表格中各行的高度，填满总体高度。具体方法是，计算表格的高度与各行的高度之和相差多少，然后除以行数，再把得到的值增加到每一行上。

如果表格的 height 属性的值为 auto，那么表格的高度等于表格中每一行的高度之和加上边框和单元格的间距。为了确定每一行的高度，用户代理会使用一套类似确定列宽的计算过程。用户代理先计算各单元格中内容的最小高度和最大高度，由此得出各行的最小高度和最大高度。然后，用户代理算出各行的高度，叠加在一起得到表格的高度。这与行内布局很像，只不过对结果不那么确定。

除了如何处理显式为表格设定的高度，以及如何处理表格中各行的高度之外，CSS 没有定义以下事项：

* 使用百分数设定单元格高度的效果。
* 使用百分数设定行和行组高度的效果。
* 跨行单元格对所跨行的高度有何影响（除了单元格在这些行中之外）。

可以看到，表格的高度很大程度上由用户代理确定。历史证明，不同的用户代理采用的方法有所不同，因此最好尽量不为表格设定高度。

<br>

## 3. 对齐方式

有意思的是，相对于单元格和行的高度，定义单元格中内容的对齐方式要容易得多。甚至垂直对齐也很容易定义，而这很有可能对行得高度带来影响。

横向对齐是最简单得。若想对齐单元格中的内容，使用 text-align 属性。其实，单元格相当于块级框，因此单元格中的内容根据 text-align 属性的值对齐。

单元格中内容的纵向对齐方式使用 vertical-align 属性设定。这个属性的值很多都与行内内容的纵向对齐一样，不过应用到单元格上有不同的作用。下面三个值是最容易理解的：

top

​      单元格中内容的顶部与行的顶部对齐。在跨行的单元格中，内容的顶部与所跨的第一行的顶部对齐。

bottom

​      单元格中内容的底部与行的底部对齐。在跨行的单元格中，内容的底部与所跨的最后一行的底部对齐。

middle

​      单元格中内容的中线与行的中线对齐。在跨行的单元格中，内容的中线与所跨各行整体的中线对齐。

<br>

这三个值的效果如下图所示，使用的样式和标记如下：

```css
table {
    table-layout: auto;
    width: 20em;
    border-collapse: separate;
    border-spacing: 3px;
}

td {
    border: 1px dashed gray;
    background: silver;
    padding: 0;
}

div {
    border: 1px dashed gray;
    background: white;
}

#r1c1 {
    vertical-align: top;
    height: 10em;
}

#r1c2 {
    vertical-align: middle;
}

#r1c3 {
    vertical-align: bottom;
}
```

```html
<table>
    <tr>
        <td id="r1c1">
            <div>
                The contents of this cell are top-aligned.
            </div>
        </td>
        <td id="r1c2">
            <div>
                The contents of this cell are middle-aligned.
            </div>
        </td>
        <td id="r1c3">
            <div>
                The contents of this cell are bottom-aligned.
            </div>
        </td>
    </tr>
</table>
```

![](https://front-end-1257950569.cos.ap-guangzhou.myqcloud.com/CSS%20%E6%9D%83%E5%A8%81%E6%8C%87%E5%8D%97%EF%BC%88%E7%AC%AC4%E7%89%88%EF%BC%89/%E7%AC%AC14%E7%AB%A0%EF%BC%9ACSS%20%E4%B8%AD%E7%9A%84%E8%A1%A8%E6%A0%BC%E5%B8%83%E5%B1%80/%E7%BA%B5%E5%90%91%E5%AF%B9%E9%BD%90%E5%8D%95%E5%85%83%E6%A0%BC%E4%B8%AD%E7%9A%84%E5%86%85%E5%AE%B9.png)

每种对齐效果都是通过自动增加单元格的内边距实现的。在上图中的第一个单元格里，单元格的下内边距变成单元格框的高度与单元格中内容的高度之差。在第二个单元格中，单元格的上下内边距被置为同样的大小，从而让内容在单元格中纵向居中。在第三个单元格中，调整的是单元格的上内边距。

第四个可用的值是 baseline，这个值比前三个稍微复杂一些：

baseline

​             单元格的基线与行的基线对齐。在跨行的单元格中，单元格的基线与所跨的第一行的基线对齐。

这个值的作用最好通过图示说明（见下图）。

![](https://front-end-1257950569.cos.ap-guangzhou.myqcloud.com/CSS%20%E6%9D%83%E5%A8%81%E6%8C%87%E5%8D%97%EF%BC%88%E7%AC%AC4%E7%89%88%EF%BC%89/%E7%AC%AC14%E7%AB%A0%EF%BC%9ACSS%20%E4%B8%AD%E7%9A%84%E8%A1%A8%E6%A0%BC%E5%B8%83%E5%B1%80/%E5%90%91%E5%9F%BA%E7%BA%BF%E5%AF%B9%E9%BD%90%E5%8D%95%E5%85%83%E6%A0%BC%E7%9A%84%E5%86%85%E5%AE%B9.png)

行的基线由行中最低的单元格初行基线（即第一行文本的基线）定义。因此，在上图中，行的基线由第三个单元格定义，因为那个单元格的初行基线最低。前两个单元格中第一行文本的基线与这条行的基线对齐。

与上对齐、居中对齐和下对齐一样，基线对齐也是通过调整单元格的上下内边距实现的。如果一行中的单元格都不采用基线对齐，那么行就没有基线，因为根本就不需要有。

对齐行中各单元格里内容的具体过程如下：

1. 如果有单元格的基线对齐的，找出行的基线，放置沿基线对齐的单元格中的内容。
2. 放置靠上对齐的单元格中的内容。现在行的高度是临时的，又已经确定内容位置的单元格中底边对齐的那个单元格确定。
3. 如果余下的单元格中有居中对齐或靠下对齐的，而且内容的高度比行的临时高度大，那么行的高度将增大，覆盖这些单元格中最高的那个。
4. 放置余下的单元格中的内容。如果单元格中内容的高度比行的高度小，单元格的内边距将增大，与行的高度保持一致。

vertical-align 的其他值，即 sub、super、text-top 和 text-bottom，应用到单元格上时应该被忽略，全都视作 baseline，有时也可能视作 top。






































































